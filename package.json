{
  "name": "prompt-generator-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ig:login": "node playwright/login.js",
    "ig:collect": "node playwright/collect-reels.js",
    "ig:save": "node playwright/save-json.js",
    "check": "node --check playwright/login.js && node --check playwright/collect-reels.js && node --check playwright/parser.js && node --check playwright/save-json.js && node --check playwright/utils.js"
  },
  "dependencies": {
    "playwright": "^1.48.2"
  }
}
